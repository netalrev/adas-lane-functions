"""
src/detectors/__init__.py
==========================
Detectors package — online inference engines that consume raw camera frames
and produce timestamped, ID-stable detections.

Sub-packages
------------
lane/     — LaneManager and all lane-detection strategy plugins.
vehicle/  — TargetDetector (YOLO ONNX), KalmanTracker, TrackManager.
"""
